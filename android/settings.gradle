rootProject.name = 'TodoMasterApp'

// Redirect Gradle project cache outside OneDrive to prevent file-lock issues
// (OneDrive sync locks files during atomic renames which Gradle relies on)
gradle.projectCacheDir = new File("C:/gradle-caches/TodoMasterApp")

apply from: file("../node_modules/@react-native-community/cli-platform-android/native_modules.gradle"); applyNativeModulesSettingsGradle(settings)
include ':app'
includeBuild('../node_modules/@react-native/gradle-plugin')
